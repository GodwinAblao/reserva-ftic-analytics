[variables]
PYTHON_VERSION = "3.11"

[phases.setup]
nixPkgs = ['python311', 'python311Packages.pip']

[phases.build]
cmds = ["pip install -r requirements.txt"]

[start]
cmds = ["python -m uvicorn app:app --host 0.0.0.0 --port $PORT"]
